# Clawdbot gateway URL (default: local gateway)
NEXT_PUBLIC_GATEWAY_URL=http://127.0.0.1:18789

# Gateway auth token (from ~/.clawdbot/.env CLAWDBOT_GATEWAY_TOKEN).
# Leave empty if your gateway has no auth.
CLAWDBOT_GATEWAY_TOKEN=

# Optional: use JARVIS Edge Function as backend (hosted JARVIS via Railway + Supabase).
# When set, chat and health use Edge → Railway gateway.
# NEXT_PUBLIC_JARVIS_EDGE_URL=https://YOUR_PROJECT_REF.supabase.co/functions/v1/jarvis
# If Edge returns 401 Unauthorized: set JARVIS_AUTH_TOKEN here to the same value as the Edge secret.
# JARVIS_AUTH_TOKEN=

# Optional: chat request timeout in ms (default 90000). Use 90000 for farm/Nano.
# NEXT_PUBLIC_JARVIS_CHAT_TIMEOUT_MS=90000

# Optional: password-protect the UI (only you can access). Set both to enable.
# JARVIS_UI_PASSWORD=jarvis2025
# JARVIS_UI_AUTH_SECRET=random-32-char-or-longer-secret-for-signing-sessions
